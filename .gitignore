# NEVER commit secrets or live node state
.credentials.json
**/.credentials.json
*.credentials.json
honcho.json
!honcho.template.json
MEMORY.md
USER.md
!*.template.md
hooks/cache/
**/cache/
*.lock
.last-refresh
.last-cleanup
sessions/
shell-snapshots/
projects/
session-env/
backups/
*.bak
*.bak-*
.env
*.pem
*.key
*token*
*secret*

# Local convenience symlink to bridge/ (created per-node, not part of the template)
/telegram_bot

# Per-node bridge runtime state (health/sessions/tasks snapshots — mechanism repo, not node state)
/.telegram_bot/

# Python bytecode
__pycache__/
*.pyc
