interface:
  display_name: "Fleet Web Routing"
  short_description: "Firecrawl search/scrape/Developer Index routing; explicit SearXNG fallback"
  default_prompt: "Use $web-routing to route general search to Firecrawl Search, known-URL reads to Firecrawl scrape, and developer artifact questions to the Firecrawl Developer Index; fall back to SearXNG only when explicitly justified."
